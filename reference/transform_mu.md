# Transform nest association to precision (mu)

Transform nest association to precision (mu)

## Usage

``` r
transform_mu(parameters)
```

## Arguments

- parameters:

  A named list containing the parameters for a given agent

## Value

A named list containing the transformed parameters
