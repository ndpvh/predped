% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getters_setters.R, R/predped.R
\name{weights}
\alias{weights}
\alias{weights<-}
\alias{weights,predped-method}
\alias{weights<-,predped-method}
\title{Getter/Setter for the \code{weights}-slot}
\usage{
weights(object, ...)

weights(object) <- value

\S4method{weights}{predped}(object)

\S4method{weights}{predped}(object) <- value
}
\arguments{
\item{object}{An instance of the \code{\link[predped]{predped-class}}.}

\item{...}{Arguments passed to the methods of this generic}

\item{value}{Value with which to replace the original value of the 
\code{weights} slot.}
}
\description{
Works for \code{\link[predped]{predped-class}}.
}
\examples{
# Initialize a predped model
my_background <- background(shape = rectangle(center = c(0, 0), 
                                              size = c(2, 2)), 
                            objects = list())

my_model <- predped(setting = my_background, 
                    archetypes = c("BaselineEuropean", 
                                   "DrunkAussie"))

# Access the archetypes slot 
weights(my_model)

# Change the archetypes slot
weights(my_model) <- c(0.9, 0.1)
weights(my_model)

}
\seealso{
\code{\link[predped]{predped-class}}
}
