% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace.R
\name{initialize,trace-method}
\alias{initialize,trace-method}
\title{Constructor for the \code{\link[predped]{trace-class}}}
\usage{
\S4method{initialize}{trace}(
  .Object,
  setting = NULL,
  id = character(0),
  time_step = 0.5,
  states = list(),
  variables = list()
)
}
\arguments{
\item{setting}{Object of the \code{\link[predped]{background-class}}. Defaults
to \code{NULL}, in which case the constructor will throw an error as this is
a slot that needs to be specified.}

\item{id}{Character that serves as an identifier for the trace. Defaults to 
an indicator \code{"trace"} pasted together with a random 5-letter string.}

\item{time_step}{Numerical denoting the time that passes at each iteration in
seconds. Defaults to \code{0.5}.}

\item{states}{List of lists containing the state of the 
\code{\link[predped]{agent-class}}s at each iteration. Defaults to an empty 
list.}

\item{variables}{List of list of user-specified variables that are used to 
control the simulation (see the \code{fx} argument of 
\code{\link[predped]{simulate}}) at each iteration. Defaults to an empty list.}
}
\value{
Object of the \code{\link[predped]{trace-class}}
}
\description{
Constructor for the \code{\link[predped]{trace-class}}
}
\examples{
# Create a background 
setting <- background(
  shape = rectangle(center = c(0, 0), size = c(5, 5)),
  objects = list(circle(center = c(0, 0), radius = 1))
)

# Initialize trace
my_trace <- trace(
  id = "my trace"
  time_step = 1,
  setting = setting
)

# Access some of the slots that were specified
my_trace@id
my_trace@time_step

}
\seealso{
\code{\link[predped]{agent-class}},
\code{\link[predped]{background-class}},
\code{\link[predped]{state-class}},
\code{\link[predped]{trace-class}}
\code{\link[predped]{simulate}}
}
\concept{classes}
